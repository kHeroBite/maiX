🐛 OneNote 녹음 종료 1분 hang 제거 — 최종요약 fire-and-forget 분리 + WebSocket close 타임아웃 by claude-opus-4-8

## 근본 원인
- 주 hang: StopRecordingAsync() → FinalSummarizeAsync() → OpenAI API 최대 90초 동기 await
- 부 hang: _ws.CloseAsync(CancellationToken.None) — WebSocket stall 시 무한 대기 (L-451)
- 오해 보정: 사용자 추정("점진 저장 부재 → 일괄 저장 hang")은 코드 탐색으로 반증 (L-526)
  TriggerRealtimePersist(2.5s debounce)는 이미 구현되어 있었음

## 수정 내용
- OpenAiRealtimeSttService.cs: CloseAsync CancellationToken.None → 5초 타임아웃 CT (L-451 준수)
- CumulativeSummaryService.cs: FinalSummarizeAsync 타임아웃 90s → 30s
- OneNoteViewModel.cs: IsFinalSummaryInProgress ObservableProperty 추가
  + FinalSummarizeAsync Task.Run fire-and-forget 분리 (Stop 즉시 UI 반환)
  + Task.Run 내부 try-catch + Dispatcher.InvokeAsync로 플래그 복구 (L-379/L-374 준수)

## 트레이드오프
fire-and-forget 분리 → 앱 강제 종료 시 최종요약 유실 가능
완화: IsFinalSummaryInProgress 플래그 UI 노출 + 백그라운드 완료 시 persist 호출

## 신규 교훈
- L-525: Stop 경로 외부 API fire-and-forget 분리 + IsFinalSummaryInProgress 패턴
- L-526: oplan 가설 검증 — 사용자 추정 원인 코드 교차 확인 필수 (L-419 연계)

🤖 Generated with [Claude Code](https://claude.com/claude-code) (claude-opus-4-8)

Co-Authored-By: Claude <noreply@anthropic.com>
